public class PalavrasReservadas {
    public static void main(String[] args) {
        // Palavras reservadas em Java, classificadas e categorizadas:

        // Controle de fluxo:
        // if, else, switch, case, default, while, do, for, break, continue, return

        // Modificadores de acesso:
        // public, private, protected, default

        // Modificadores de classe, métodos ou variáveis:
        //abstract, class, extends, implements, final, native, static, synchronized, 
        //transient, volatile, interface, strictfp, new, this, super

        // Classes, objetos e interfaces:
        // class, interface, extends, implements, new, this, super

        // Tratamento de exceções:
        // try, catch, finally, throw, throws

        // Pacotes:
        // package, import

        // Tipos primitivos:
        // byte, short, int, long, float, double, char, boolean, void  

        // Valores literais:
        // true, false, null

        // Controle de threads:
        // synchronized

        // Variáveis e referências:
        // void, static, final, transient, volatile, super, this

        // Operadores:
        // instanceof, native, strictfp, assert, enum

        //Tratamento de erros
        //assert, catch, finally, throw, throws, try

        //palavras reservadas não utilizadas:
        //const, goto

    }
}
